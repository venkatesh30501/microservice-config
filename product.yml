spring:
  application:
    name: product

  datasource:
    url: jdbc:mysql://mysql_db:3306/product_db?createDatabaseIfNotExist=true
    username: root
    password: root@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

server:
  port: 8087

product:
  message: Welcome to the Product Service!
  apikey: api_key_54321
