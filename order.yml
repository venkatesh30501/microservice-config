spring:
  application:
    name: order

  datasource:
    url: jdbc:mysql://mysql-db:3307/order_db?createDatabaseIfNotExist=true
    username: root
    password: root@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true


server:
  port: 8085

order:
  message: Welcome to the Order Service!
  apikey: api_key_2468099
